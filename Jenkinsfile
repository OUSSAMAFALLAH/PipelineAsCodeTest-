pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'Build Completed'
        sh ''
      }
    }
    stage('Test') {
      steps {
        echo 'Test Completed'
      }
    }
    stage('Deploy') {
      steps {
        sh 'date'
        echo 'Deploy Completed'
      }
    }

  }
}
